# ─────────────────────────────────────────────────────────────────
# vision-service 依赖（轻量版，可立即本地运行）
# 安装: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────

# HTTP 框架
fastapi>=0.110.0

# ASGI 服务器（standard 额外包含 uvloop/httptools 加速）
uvicorn[standard]>=0.29.0

# 向量检索库（CPU 版，约 30MB，Apple Silicon/x86 均可通过 pip 安装）
faiss-cpu>=1.8.0

# 数值计算（faiss 依赖）
numpy>=1.26.0

# 图片处理（PIL）
pillow>=10.0.0

# 处理 multipart/form-data（FastAPI 文件上传依赖）
python-multipart>=0.0.9

# 数据校验 v2（Pydantic 语法全面升级，schemas.py 使用 v2 API）
pydantic>=2.6.0

# 环境变量驱动的配置（config.py）
pydantic-settings>=2.2.0

# 异步 HTTP 客户端（search.py 拉取 image_url）
httpx>=0.27.0

# ─────────────────────────────────────────────────────────────────
# 真实视觉嵌入（DINOv3 ViT-B/16，86M 参数 / 768 维 / 权重 ~350MB）
# 安装后设置 VISION_USE_MOCK=false 启用
# ─────────────────────────────────────────────────────────────────
torch>=2.4.0           # PyTorch — DINOv3 推理引擎
transformers>=4.45.0   # HuggingFace Transformers — 4.45+ 原生支持 DINOv3
accelerate>=0.30.0     # 可选：device_map="auto" + 多设备加速
